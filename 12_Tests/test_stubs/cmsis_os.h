#ifndef CMSIS_OS_H
#define CMSIS_OS_H

// Minimal placeholder so code that references os functions/types compiles.
// Define whatever the production code uses; if it uses nothing, this can stay empty.

typedef void osThreadId;
typedef void osMutexId;
typedef void osSemaphoreId;

#endif // CMSIS_OS_H